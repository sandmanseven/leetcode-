//time complexity O(n)
//space complexity O(1)
public int singleNumber(int [] nums){
   int result=0;
   for(int i=;i<nums.length;i++){
    result=result^nums[i];
    }
    return result;
    }
    /*for this problem you need to find the only one that appear only once,because one number
    XOR itself is 0,so we need to traverse the array,that XOR one by one and finally we can find
    the single number,0 XOR num=num.
    */
